# Notification

# Step 1 : To run the app use command line: `docker compose up -d --build`

# Step 2 : If you want to rebuild the apps:

# - First we have to take down the Docker Image first: `docker compose down`

# - Do the Step 1

# Step 3 : To access the API Menu: `http://localhost:{PORT}/api/`

# The default port is 8080
